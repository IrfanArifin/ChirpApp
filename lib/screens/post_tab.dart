import 'package:flutter/material.dart';

class PostTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Ini halaman tambah postingan", style: TextStyle(fontSize: 20)),
    );
  }
}
